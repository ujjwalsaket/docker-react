sudo: required
labguage: generic
services:
  - docker

before_install:
  - docker build -t ujjwalsaket/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ujjwalsaket/docker-react npm run test -- --coverage
